JavaScript and jQuery based single line typing_effect with rotateable array of strings

You need to include the typing-effect.js in html page and define an empty dom element such as div with id defined.


then use typing_effect(['This is a sample string for testing single line typing effect!', 'just another line for typing effect'], '#typing_line'); in either script tag or your .js file

first parameter: string to be used for typing effect
second parameter: id of the dom element

